# CHO-271: context-engineering-tool-result-stubs — tasks

## 1. The curation module (pure, no I/O)

- [x] 1.1 New `backend/app/agent/curation.py`: `_FRAME = "[App note — generated by the app, not typed by the user]"`, `_KEEP_VERBATIM = {"open_report_form", "raise_support_ticket"}`, `_RENDERERS` (tool name → envelope → text), `_ALLOWED_KEYS` (per-renderer key whitelist), `_MAX_NOTE_IDS = 40`
- [x] 1.2 `watermark(thread)` → `seq` of the newest `user_text` turn (0 when there is none); depends on `user_text` turns only, so it cannot move between rounds of a turn
- [x] 1.3 `stub_blocks(turn)` → replacement blocks or `None` (keep verbatim). Replaces only the `tool_result` **`content`** string; `type` / `tool_use_id` / `is_error` copied through. Never raises: a malformed, legacy (`"{}"`) or missing-`meta` envelope falls back to the generic template
- [x] 1.4 Never-curate rules: `meta["is_error"] is True` (any tool), `open_report_form`, `raise_support_ticket`, and any unregistered tool name (fail-open)
- [x] 1.5 Artifact renderers — `get_holdings`, `get_money_transactions` (incl. the `partial` clause), `get_brokerage_rates`, and the file envelopes (`get_pnl_report`, `get_ledger_report`, `get_capital_gains_report`, `download_contract_note`; download vs email wording, **never** echo `emailMasked`). K pinned to 1 per class in code
- [x] 1.6 KB renderer: keep the matched `question` strings, drop `answer` / `tat` / `score`, end with the re-search clause
- [x] 1.7 `get_report_columns` renderer: count only — labels dropped — ending with "call `get_report_columns` again before describing any report column"
- [x] 1.8 `list_contract_notes` renderer: every `id ↔ date/segment` pair, cap the 40 newest with an explicit omission line, and **state the ~600-second id expiry plus the recovery** ("if a download bounces, call `list_contract_notes` again"); drop `month` / `badge` / JSON framing. `n == 0` variant
- [x] 1.9 Every template ends with an actionable recovery clause; no template contains a ₹ amount, price, scrip name, file token, credential or upstream field — enforced by the per-renderer key whitelist, not by an output regex
- [x] 1.10 `curate(thread)` returns the transform closure, memoising per `turn.seq` in `thread.curated`; `messages_for_model(thread)` = `thread.messages(transform=curate(thread))`

## 2. Store seam (lossless default unchanged)

- [x] 2.1 `store.py`: add non-persisted `Thread.curated: dict[int, list[dict]]` (transient like `first_name` / `name_fetched`)
- [x] 2.2 `store.py`: optional keyword-only `transform=` on `Thread.messages()`; `None` ⇒ byte-identical to today. Merge + `tool_result`-first partition logic stays in this one place

## 3. Loop, config, observability

- [x] 3.1 `config.py`: `agent_context_curation()` (`AGENT_CONTEXT_CURATION`, **default off**), styled after `tracing_enabled()`. No `AGENT_CURATION_KEEP_TURNS` knob — K lives in code
- [x] 3.2 `loop.py:298-304`: one call site — `agent_curation.messages_for_model(thread)` when the flag is on, `thread.messages()` otherwise
- [x] 3.3 `tracing.py`: optional `extra: dict | None` on `observe_model_round`, merged into the `llm` span metadata; loop passes `curated_turns` + `history_tokens_est` (counts only, no text)
- [x] 3.4 `.env.example`: document `AGENT_CONTEXT_CURATION` (default off, flipped on after the CHO-276 evals)

## 4. Unit tests — `backend/tests/test_agent_curation.py`

- [x] 4.1 Golden stub string per renderer: holdings ok/empty; money ok/partial/empty; brokerage; file download; file email; KB; columns; notes n=6 / n=45 / n=0; generic fallback
- [x] 4.2 Stub preserves `type == "tool_result"`, `tool_use_id` and `is_error` — only `content` differs
- [x] 4.3 `is_error=True` is never stubbed, for every tool name
- [x] 4.4 `open_report_form` and `raise_support_ticket` results are never stubbed
- [x] 4.5 Notes stub contains **every** id for n ≤ 40; for n = 45 keeps the 40 newest plus the omission sentence; states the id expiry
- [x] 4.6 K=1 boundary: the current turn's results are verbatim, the previous turn's are stubbed
- [x] 4.7 Monotonicity: `json.dumps` of the pre-watermark prefix is identical at turns N+1, N+2, N+3
- [x] 4.8 Within-turn byte stability: appending a second `tool_result` mid-turn leaves the prefix through the newest `user_text` block byte-identical to the previous round's
- [x] 4.9 Unregistered tool name → verbatim (fail-open)
- [x] 4.10 Legacy / garbage content (`"{}"`, missing `meta["tool_name"]`, non-JSON) → generic stub or verbatim, never raises
- [x] 4.11 Flag off → `messages_for_model(thread)` is byte-identical to `thread.messages()`
- [x] 4.12 Store untouched: after building the view, every `turn.content` is byte-identical to what was appended (and no stub object aliases a stored block)
- [x] 4.13 No-currency guard: every renderer's key whitelist contains only count / length / customer-facing-label / ISO-date / id keys, and no amount-bearing key (`amt`, `landed`, `current`, `invested`, `pnl`, `ltp`, `abp`, `day`, `rates`, …) appears in any whitelist
- [x] 4.14 `tool_use` / `tool_result` pairing preserved: for a parallel-tool round, every `tool_use` id in the assistant message has exactly one matching `tool_result` block in the curated array
- [x] 4.15 Loop integration — **landed in a new `backend/tests/test_agent_curation_loop.py`** instead of `test_agent_loop.py`, which two sibling changes own concurrently: after two user messages, `fake.calls[-1]["messages"]` shows the earlier artifact result as a stub, with block count and `tool_use_id`s preserved
- [x] 4.16 `tickets.render_transcript` and `ticket_call_is_user_initiated` are byte-identical with the flag on and off (they read `thread.turns`, not the view)

## 5. Regression sweep

- [x] 5.1 `cd backend && uv run pytest` green — in particular the store's byte-identical replay, flow-event merge/partition and restart-prefix tests (all use the no-transform default), and the loop's parallel-results, error-bounce, short-circuit-continuation, cap and reset tests
- [x] 5.2 Confirm no `prompt_snapshots` row and no `prompt_hash` change (this change edits no prompt text)

## 6. Eval gate (CHO-276) and the flag flip

- [ ] 6.1 Merge with `AGENT_CONTEXT_CURATION=0`
- [ ] 6.2 Run the CHO-276 routing evals with the flag **off** and **on**, 3 attempts each, recording `input_tokens` from `agent_traces` for the cost delta
- [ ] 6.3 Gate: mid-thread seeded form, no-stale-figures, turn-2-fact-recalled-at-turn-14, report-columns compliance and KB pronoun follow-up each ≥2/3 **with no regression vs off**; note-id survival and the ticket affirmation guard **3/3** (the note case must complete inside 10 minutes — the ids expire); **zero** abstention-regex matches
- [ ] 6.4 Deploy the new `backendvX.Y.Z` (frontend tag untouched), flip `AGENT_CONTEXT_CURATION=1` on prod (user-gated: `docker rm -f jini-backend` + `docker run … -e AGENT_CONTEXT_CURATION=1`)
- [ ] 6.5 Confirm on prod traces for ≥5-turn threads: `history_tokens_est` down ≥60%, `curated_turns` > 0, per-turn `cost_inr` at turn ≥8 down again, no rise in error or abstention-shaped replies
- [ ] 6.6 Follow-up commit: change the default to on
- [ ] 6.7 Record whether the KB pronoun follow-up and the mid-thread seeded form pass — this decides whether CHO-263 (recitation block) is built at all

## 7. Ship & sync

- [ ] 7.1 `git-sync` with issue key CHO-271
- [ ] 7.2 `linear-connector` — In Progress at implementation, summary comment + In Review at PR, Done at merge (note the flag state at merge and the pending default flip)
