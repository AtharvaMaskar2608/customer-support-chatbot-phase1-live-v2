# Environment / secrets — never commit real tokens
.env*
!.env.example

# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
*.egg-info/

# Node
node_modules/

# Build output
dist/

# Playwright artifacts
playwright-report/
test-results/
playwright/.cache/
.gstack/

# Local editor/agent settings
.claude/settings.local.json
.vscode/

# local embed demo — carries a live session token, never commit
jini-embed-demo.html

# deploy image tarballs (docker save output — build artifacts, never commit)
jini-*.tar.gz

# routing-eval sweep reports (CHO-276) — per-run artifacts, attached to Linear
# rather than committed
backend/evals/reports/
